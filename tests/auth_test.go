package tests

import "testing"

func TestAuthentication(t *testing.T) {
	// _, err := auth.Authenticate(auth.LoginRequest{
	// 	Username: "admin",
	// 	Password: "password",
	// })
	// if err != nil {
	// 	t.Fatalf("Expected successful authentication, got error: %s", err)
	// }
}
